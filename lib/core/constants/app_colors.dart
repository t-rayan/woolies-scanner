import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const black = Color(0xFF000000);
  static const darkGrey = Color(0xFF1A1A1A);
  static const grey = Color(0xFF757575);
  static const lightGrey = Color(0xFFEEEEEE);
  static const white = Color(0xFFFFFFFF);
  
  static const background = Color(0xFFFFFFFF);
  static const surface = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF000000);
  static const textSecondary = Color(0xFF757575);
  static const border = Color(0xFFE0E0E0);
  
  // Use these for semantic meaning but keep them minimal
  static const accent = Color(0xFF000000);
  static const error = Color(0xFF000000); // Minimal B&W
}
